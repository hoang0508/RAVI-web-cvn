export enum LabelStatusType {
  APPROVED = "success",
  PENDING = "pending",
  REJECT = "cancel",
}

export enum Vouncher {
  VOUNCHER_USE = "use",
  VOUNCHER_EXPIRED = "exprired",
  VOUNCHER_TO_USE = "touse",
}
